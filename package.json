{
  "name": "SilentVictoryEncounter",
  "version": "0.1.0",
  "homepage": "http://silentvictory.fastwombat.com",
  "private": true,
  "devDependencies": {
    "custom-react-scripts": "0.0.23"
  },
  "dependencies": {
    "classnames": "2.2.5",
    "lodash": "4.17.4",
    "mobx": "^3.1.0",
    "mobx-react": "^4.1.0",
    "mobx-react-router": "^3.1.2",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-router": "^3.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "configure": "aws s3 mb s3://silentvictory.fastwombat.com && aws s3 website s3://silentvictory.fastwombat.com/ --index-document index.html --error-document error.html",
    "predeploy-client": "npm run build",
    "deploy": "npm run deploy-client",
    "deploy-client": "aws s3 sync build/ s3://silentvictory.fastwombat.com"
  }
}
